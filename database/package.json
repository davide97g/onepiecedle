{
  "name": "onepiecedle-database",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "scrape:links": "yarn build && node dist/src/links.js",
    "scrape:characters": "yarn build && node dist/src/characters.js",
    "scrape:saga": "yarn build && node dist/src/saga.js",
    "scrape:arcs": "yarn build && node dist/src/arcs.js",
    "scrape:clean": "yarn build && node dist/src/clean.js",
    "scrape:haki": "yarn build && node dist/src/haki.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cli-progress": "^3.12.0",
    "puppeteer": "^22.9.0"
  },
  "devDependencies": {
    "@types/cli-progress": "^3.11.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
